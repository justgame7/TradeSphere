import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cryptoscreener.ichimoku',
  appName: 'TradeSphere',
  webDir: 'www'
};

export default config;
